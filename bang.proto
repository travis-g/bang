syntax = "proto3";
package main;

message Bang {
	string name = 1;
	string format = 2;
	string description = 3;

	enum EscapeMethod {
		QUERY_ESCAPE = 0;
		PASS_THROUGH = 1;
		PATH_ESCAPE = 2;
	}

	bool unescaped = 4;
	EscapeMethod escape_method = 5;
}
